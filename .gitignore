# Runtime session state created by the handoff skill itself (default location).
# Per-machine, regenerated per session — not inherited by a fork.
.handoffs/

# Per-machine Claude Code settings.
.claude/settings.local.json

# Design notes / specs — transient working substrate, not needed long-term.
notes/
